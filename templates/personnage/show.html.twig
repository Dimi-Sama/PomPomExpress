{% extends 'baseAdmin.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block body %}
    <h1>Personnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ personnage.nom }}</td>
            </tr>
            <tr>
                <th>Etoile</th>
                <td>{{ personnage.etoile }}</td>
            </tr>
            <tr>
            <th>attribut</th>
                <td>{{ personnage.attribut }}</td>
            </tr>
            <tr>
            <th>Voie</th>
                <td>{{ personnage.voie }}</td>
            </tr>
            <th>Ultime</th>
                <table class="table">
		        <thead><tr><th>{{ personnage.ultime.nom }}</th></tr></thead>
                <tbody><td>{{ personnage.ultime.description }}</td></tbody>
            </tr>
            <th>Competence </th>
                <table class="table">
		        <thead><tr><th>{{ personnage.competence.nom }}</th></tr></thead>
                <tbody><td>{{ personnage.competence.description }}</td></tbody>
            </tr>
            <th>Talent</th>
                <table class="table">
		        <thead><tr><th>{{ personnage.talent.nom }}</th></tr></thead>
                <tbody><td>{{ personnage.talent.description }}</td></tbody>
            </tr>
            <th>Normale</th>
                <table class="table">
		        <thead><tr><th>{{ personnage.normale.nom }}</th></tr></thead>
                <tbody><td>{{ personnage.normale.description }}</td></tbody>
            </tr>
            

        </tbody>
    </table>

    <a href="{{ path('app_personnage_index') }}">back to list</a>

    <a href="{{ path('app_personnage_edit', {'id': personnage.id}) }}">edit</a>

    {{ include('personnage/_delete_form.html.twig') }}
{% endblock %}
