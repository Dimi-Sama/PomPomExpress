<form method="post" action="{{ path('app_voie_delete', {'id': voie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voie.id) }}">
    <button class="btn">Delete</button>
</form>
