{% extends 'baseAdmin.html.twig' %}

{% block title %}Voie{% endblock %}

{% block body %}
    <h1>Voie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ voie.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voie_index') }}">back to list</a>

    <a href="{{ path('app_voie_edit', {'id': voie.id}) }}">edit</a>

    {{ include('voie/_delete_form.html.twig') }}
{% endblock %}
